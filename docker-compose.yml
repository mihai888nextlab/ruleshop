services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ruleshop
      POSTGRES_PASSWORD: ruleshop
      POSTGRES_DB: ruleshop
    ports:
      - "5432:5432"
    volumes:
      - ruleshop_pg:/var/lib/postgresql/data

volumes:
  ruleshop_pg:
