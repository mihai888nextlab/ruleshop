# Storefront — talks to the control plane over HTTP and nothing else.
# Copy to apps/storefront/.env. Note there is deliberately no DATABASE_URL
# here: this app has no database driver installed.

CONTROL_PLANE_URL="http://localhost:3001"
